# PostCAD Protocol Node — environment variables
#
# Copy to .env and adjust as needed.
# docker run --env-file .env postcad-node

# Address and port the HTTP service binds to.
POSTCAD_ADDR=0.0.0.0:8080

# Log level for the service binary (trace|debug|info|warn|error).
RUST_LOG=info
